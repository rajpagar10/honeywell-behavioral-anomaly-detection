# Operational Scripts

The installed `badp` command is the supported Milestone 2 CLI. Dataset
generation, model training, evaluation, simulation, and demo scripts will be
added with their owning milestones rather than as nonfunctional stubs.
